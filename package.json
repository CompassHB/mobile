{
  "name": "mobile",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start --project-path build",
    "clean": "rm -rf build",
    "postinstall": "typings install",
    "watch": "tsc -w",
    "ios": "react-native run-ios",
    "android": "react-native run-android",
    "test": "jest"
  },
  "dependencies": {
    "react": "15.2.1",
    "react-native": "0.30.0"
  },
  "devDependencies": {
    "jest-cli": "^13.2.3",
    "typings": "^1.3.2"
  }
}
